const Footer = () => {
    return (
        <footer className="bg-black text-white">
            <p>
                &copy; {new Date().getFullYear()} Andrew Santos
            </p>
        </footer>
    )
}

export default Footer;
